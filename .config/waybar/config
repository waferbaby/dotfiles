{
    "layer": "top",
    "margin-top": 0,
    "margin-bottom": 0,
    "margin-left": 0,
    "margin-right": 0,
    "spacing": 0,

    "modules-left": [
        "niri/window"
    ],

    "modules-right": [
        "pulseaudio",
        "network",
        "battery",
        "clock"
    ],

    "pulseaudio": {
      "format": "{icon} {volume}%",
      "format-muted": "",
      "format-icons": {
        "default": ["", ""]
      },
      "scroll-step": 1
    },

    "network": {
        "format-wifi": "",
        "format-ethernet": "",
        "format-disconnected": "⚠",
    },

    "battery": {
      "states": {
        "warning": 30,
        "critical": 15
      },
      "format": "{icon} {capacity}% ({time})",
      "format-icons": ["", "", "", "", ""]
    },

    "clock": {
      "interval": 60,
      "format": "{:%B %d / %I:%M}",
    }
}
